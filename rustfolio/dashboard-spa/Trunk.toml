[build]
dist = "/assets/dashboard"
public_url = "/assets/dashboard/"
filehash = false
release = true

[serve]
address = "0.0.0.0"
port = 8081
open = false

[tools]
# Pin de wasm-bindgen (chaîne, pas une table)
wasm_bindgen = "0.2.103"

# Choix de wasm-opt (toujours une CHAÎNE ici). 
# Si version_116 t’a posé souci, essaye une plus récente :
wasm_opt = "version_121"
